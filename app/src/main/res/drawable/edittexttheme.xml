<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Arka plan rengi -->
    <corners android:radius="5dp" />  <!-- Kenar yuvarlama -->
    <!-- İçerik ile kenar arasındaki boşluk -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />
    <stroke
        android:color="@color/brand"
        android:width="1dp" />

</shape>
